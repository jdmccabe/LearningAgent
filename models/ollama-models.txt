# Repo-contained embedding model assets
#
# The GGUF file is committed through Git LFS so the repository can be cloned
# with the embedding model asset available locally. Runtime embedding should
# use the service-free hashing embedder or optional in-process GGUF embedder.

embeddinggemma:latest -> models/ollama/embeddinggemma/embeddinggemma.gguf
