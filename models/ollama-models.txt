# Repo-contained embedding model assets
#
# GGUF files are committed through Git LFS so the repository can be cloned
# with embedding model assets available locally. The default llama-cpp runtime
# uses the BGE model below because it loads directly through llama-cpp-python.

default-llama-cpp-embedder -> models/llama-cpp/bge-small-en-v1.5-q4_k_m.gguf
embeddinggemma:latest -> models/ollama/embeddinggemma/embeddinggemma.gguf
