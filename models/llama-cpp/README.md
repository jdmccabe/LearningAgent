# llama-cpp Embedding Model

Default model:

- `bge-small-en-v1.5-q4_k_m.gguf`

Source:

- Hugging Face: `CompendiumLabs/bge-small-en-v1.5-gguf`

This GGUF loads directly through `llama-cpp-python` and is used as the default `llama-cpp` embedder model. It is stored with Git LFS.
